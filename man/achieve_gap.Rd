% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/achieve_gap.R
\name{achieve_gap}
\alias{achieve_gap}
\title{Fit an achievement gap trajectory model (formula interface)}
\usage{
achieve_gap(
  formula,
  group = NULL,
  random = ~1 | school/student,
  data,
  k = 6,
  bs = "cr",
  n_sim = 10000,
  conf_level = 0.95,
  grade_grid = NULL,
  verbose = TRUE
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{score ~ grade}.
Both sides must be single variable names (no transforms).}

\item{group}{A single character string naming the binary group variable
(0/1, FALSE/TRUE, or 2-level factor) indicating reference vs focal group.}

\item{random}{Random intercept structure in lme4-style notation.
Currently only nested intercepts are supported, with the default
\code{~ 1 | school/student}.}

\item{data}{A data.frame containing all variables.}

\item{k}{Basis dimension passed to \code{\link[=gap_trajectory]{gap_trajectory()}}.}

\item{bs}{Basis type passed to \code{\link[=gap_trajectory]{gap_trajectory()}}.}

\item{n_sim}{Number of posterior simulations used for simultaneous bands.}

\item{conf_level}{Confidence level for bands (e.g., 0.95).}

\item{grade_grid}{Optional numeric vector of grades/measurement occasions
at which to evaluate trajectories.}

\item{verbose}{Logical; if TRUE prints a compact model summary message.}
}
\value{
An object of class \code{"achieveGap"} as returned by \code{\link[=gap_trajectory]{gap_trajectory()}}.
}
\description{
Convenience wrapper around \code{\link[=gap_trajectory]{gap_trajectory()}} that provides a simple
formula interface: \code{score ~ grade}. The group indicator and nested
random effects are supplied via \code{group} and \code{random}.
}
\examples{
\donttest{
sim <- simulate_gap(n_students = 200, n_schools = 20, seed = 1)
fit <- achieve_gap(
  score ~ grade,
  group  = "SES_group",
  random = ~ 1 | school/student,
  data   = sim$data,
  n_sim  = 500,
  verbose = FALSE
)
summary(fit)
}

}
